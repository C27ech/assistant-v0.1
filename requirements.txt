# ============================================================================
# Assistant v0.1 —— 全部依赖（助手本体 + 随仓库附带的 4 个插件）
#
#   pip install -r requirements.txt
#
# 只想跑助手本体（不要桌面控制/截图这些），装下面「助手本体」那一段就够。
# 平台相关的依赖用 PEP 508 标记，非 Windows 上会自动跳过。
# ============================================================================

# ---------------- 助手本体 ----------------
openai>=1.40.0              # OpenAI 兼容客户端（DeepSeek / 豆包 / 本地服务都走它）
python-dotenv>=1.0.1        # 读 .env
websocket-client>=1.7.0     # QQ 渠道（OneBot 反向 WebSocket）

# ---------------- 插件：web_tools（联网查资料）----------------
requests>=2.31.0            # HTTP 请求（web_tools；controller_v2 的视觉接口也用它）
beautifulsoup4>=4.12.0      # 解析搜索页 / arXiv 的 XML
playwright>=1.40.0          # 无头浏览器读网页正文（首次用需装浏览器，见该插件 README）

# ---------------- 插件：file_tools（读文件）----------------
# 纯标准库，无需额外依赖

# ---------------- 插件：screenshot2qq（截图发 QQ）----------------
pillow>=10.0.0              # 截图 + 压缩
pyautogui>=0.9.54           # 抓屏

# ---------------- 插件：controller_v2（桌面控制）----------------
pynput>=1.7.6               # 键鼠注入
pywin32>=306; platform_system == "Windows"   # Win32 窗口/输入接口
